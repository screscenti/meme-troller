version: '3.8'

services:
  web:
    build: .
    container_name: memetroller
    ports:
      - "5000:5000"
    volumes:
      - ./static/uploads:/app/static/uploads
      - ./instance:/app/instance
    environment:
      - FLASK_ENV=production
    restart: unless-stopped
